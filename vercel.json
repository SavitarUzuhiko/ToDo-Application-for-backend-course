{
  "buildCommand": "npm install && npm run build",
  "installCommand": "npm run ins",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "http://localhost:5000/api/$1"
    }
  ]
}
